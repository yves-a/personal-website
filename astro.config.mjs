import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace 'username' and 'repo-name' with your actual info
  site: 'https://yves-a.github.io',
  base: '/personal-website',
});